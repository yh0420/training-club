%br/
- @users.each do |user|
  = link_to account_path(user.id) do
    .relation
      .card
        - if user.avatar.attached?
          = image_tag user.avatar, class: 'card_image'
        - else
          = image_tag 'default.png', class: 'card_image'
        .relation.name
          %span= user.name